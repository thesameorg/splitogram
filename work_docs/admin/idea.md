i want to have admin page. what is a correct way to do so?
i was thinking to have basic info like:
- number of active groups
- qty of users
and some other things like that.

what is the correct way to do so? make a separate backend-frontend? have something like metabase? just add a page similar to "stats"?
maybe even take a look at group's content (if i am admin) - for readonly + delete images.
