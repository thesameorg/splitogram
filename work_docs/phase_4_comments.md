- add text validation for all input fields:
    - max length
    - symbols
    - injections cleanup if needed?


- `add expense` screen
    - add group currency display to "Amount" field - in header or aside on imput field
    - everytime i change text in expense name or in amount, the "Telegram Big Button" jumps - is removed and 


- in "groups" - main screen - when i have positive balance in a group, it is shown in green - which is correct
- in a group - balances  - when i have positive balance, it is shown in RED - which is wrong!

- on main screen the "owed to you" is shown like a sum for all groups disregarding currency, and in $. 
    - as for now we do not count rates, we should show currencies for each currency in balance - multiple rows

- `settings` button is shown in grey for owner, and for not owner user it should be labeled as `info`
- each transaction & settlement amount should be shown in respect to the viewer with color:
    - if the user is not involved, it should be in neutral color
    - if i am involved, it should have corresponding color - red for minus, green for plus

- i as admin cannot kick a user
- Richard Von Mountain - ♔ Admin - @toptrader228: remove the "admin" text, just leave the crown.
