# Good ideas
- settled groups are hidden under "show N settled groups" button

- on "group" page, when there are no members, there is a big button to "invite"
- on "pages - balances" group there is a quite limited view for members with 0 balance
- there is a calendar-style selector for expense date - it is editable later and sets sorting sequence for feed.
- has "shares" split method - with INT number of shares per person
- has "comments" section on each expense - looks as a chat
- account has 15-ish lines of different notification settings
- when at least 1 member except me exists (even placeholder) background or a hove image shows arrow to "add expense" - in case there are no expenses there at all


# Good but later
- there is a nice neutral background image. maybe not applicable for light-dark theme... idk - maybe use 2 bg images? makes things stand out


# Questionable
- there is a "Friends" section with 1-on-1 balances. same "hide \ show settled" logic there
- if there are only 2 people, it sugests adding more people before adding first expense
- when adding an expense, the split method is done with a button: "paid by you and split equally". when pressed, opens a dialog with 4 "common" options + more methods button
- there are no "placeholder" users without contacts 
- no group notification settings
- there is a "add expense" button on the "groups" screen - it lets you add an expense with any existing group or create a new one


# Bad or not applicable